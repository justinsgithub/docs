/*
interface Todo {
    name: string;
    state: boolean;
}

interface ITodoService {
    add(todo: Todo): Todo;
    delete(todoId: number): void;
    getAll(): Todo[];
    getById(todoID: number): Todo;
}

var todo: Todo = {
    name: "practice typesrcipt",
    completed: false
};*/
